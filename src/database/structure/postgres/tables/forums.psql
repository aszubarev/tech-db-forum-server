CREATE TABLE forums
(
    forum_id int GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    slug CITEXT NOT NULL UNIQUE,
    user_id int NOT NULL,
    user_nickname CITEXT NOT NULL,
    title TEXT NOT NULL,
    threads int DEFAULT 0,
    posts int DEFAULT 0
);